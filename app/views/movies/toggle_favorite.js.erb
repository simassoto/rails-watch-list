var movieId = '<%= @movie.id%>'
function toggleLikeButton(movieId, innerHTML) {
  let likeButton = document.querySelector(`.like-icon[data-id='${movieId}']`)
  likeButton.classList.toggle('red');
}
toggleLikeButton(cocktailId);
